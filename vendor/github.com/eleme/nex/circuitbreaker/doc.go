// Package circuitbreaker is wrapper built on top of github.com/eleme/circuitbreaker,
// it provides some middlewares for SOA calls which implements the endpoint.Endpoint interface.
package circuitbreaker
